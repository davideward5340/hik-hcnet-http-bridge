# 安全策略

## 支持范围

安全修复以主分支最新版本为准。历史提交和厂商 SDK 本身不在本项目维护范围内。

## 报告安全问题

请通过仓库托管平台提供的私密漏洞报告功能联系维护者。报告中请包含受影响版本、复现条件、潜在影响和建议修复方式；不要包含真实设备凭据、录像或其他敏感数据。

在修复发布前，请不要公开漏洞细节。与 HCNetSDK、VideoSDK 或设备固件直接相关的问题，应同时按照厂商安全响应流程报告。

## 部署边界

服务默认仅监听 `127.0.0.1`。不要直接暴露到公网；如需跨主机访问，应在可信网络中增加身份认证、TLS、访问控制和审计。URL 可能携带设备凭据，因此应避免浏览器历史、反向代理日志和诊断日志记录完整查询字符串。
